# ML-Bot — Python dependencies
# Install: pip install -r requirements.txt
#
# PyTorch (CUDA 12.1) — swap the index URL for your CUDA version
# or remove --index-url to fall back to CPU-only wheels.
#
#   pip install torch torchvision torchaudio \
#       --index-url https://download.pytorch.org/whl/cu121

torch>=2.1.0
torchvision
torchaudio

# Core NLP / ML
transformers>=4.41.0,<5.0.0
sentence-transformers==3.0.1
accelerate>=0.26.0
bitsandbytes>=0.43.0

# PDF parsing
pymupdf>=1.24.0

# Vector search
faiss-cpu>=1.7.4

# Retrieval utilities
scikit-learn>=1.4.0
numpy>=1.26.0
nltk>=3.8.0

# UI
streamlit>=1.35.0

# Progress bars
tqdm>=4.66.0

# Data inspection (optional — used in the notebook)
pandas>=2.2.0
